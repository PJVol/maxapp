import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  badge: {
    borderRadius: 10,
    height: 20,
    minWidth: 0,
    width: 20,
  },
  badgeContainer: {
    position: 'absolute',
  },
  badgeText: {
    fontSize: 12,
    paddingHorizontal: 0,
  },
})
